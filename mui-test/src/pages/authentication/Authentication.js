import { pageStyles } from "../styles";
import Grid from "@mui/material/Grid"

function Authentication () {
    return (
        <div style={pageStyles.pages}>
            <Grid>

            </Grid>
        </div>
    )
}

export default Authentication;